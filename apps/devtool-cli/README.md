# DevTool CLI

Ferramenta de linha de comando para rotina de desenvolvimento.

## Comandos

```bash
python3 dev_tool.py plan --hours 8 --goal "Backend"
python3 dev_tool.py standup --yesterday "API" --today "Testes" --blockers "Nenhum"
```
